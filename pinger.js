var request = require('request');

var pinger = function(params) {
  // params.ratePerMin = 60
  // params.host = 'http://example-app.com'
  // params.endpoints = [
  //   'GET /example/stats/:id',
  //   'POST /example/log/:id'
  // ]
  // params.token = ':token'










}

module['exports'] = pinger;

